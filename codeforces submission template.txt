#include <bits/stdc++.h>
using namespace std;

#define all(x) (x).begin(), (x).end()
#define ll long long

inline int cix() {
	int x;
	cin >> x;
	return x;
}

void solve(){


}

int main() {
    ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
    
    solve();

    return 0;
}
